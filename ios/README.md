# ios/

Reserved for the Xcode project.

This directory will contain the Swift / SwiftUI app source code starting in
Phase 1. Create the Xcode project here using the bundle identifier and
naming conventions defined in `CLAUDE.md` and future ADRs.

Until then, this directory intentionally contains only this README.
