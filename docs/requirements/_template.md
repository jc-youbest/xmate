# F-XXX Title

One-sentence summary of what the user can do or what the feature provides.

## Flow

When <trigger, referencing a U-XXX UI node>:
- <system action, referencing a C-XXX component or S-XXX backend module if relevant>
- <next action, with inline performance / compatibility note in parentheses if needed>

When <next trigger>:
- ...

## Notes

This section is optional. Use it only when something truly does not fit in
the Flow above (rare). Keep it short.
