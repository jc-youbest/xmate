# backend/

Reserved for the custom self-hosted backend (NOT CloudKit — see
`CLAUDE.md` → Tech Stack). Language, database, and hosting choices are
deferred until backend work starts (v3+, account & social). Planned
S-XXX modules are listed in `docs/backlog.md`.

Until then, this directory intentionally contains only this README.
